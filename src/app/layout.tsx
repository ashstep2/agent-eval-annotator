import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mercor AgentEval - AI Agent Trajectory Annotation",
  description: "Mercor's specialized tool for evaluating and annotating AI agent trajectories. Review multi-step agent workflows, tool use, and decision-making quality.",
  keywords: ["Mercor", "AI annotation", "agent evaluation", "trajectory annotation", "RLHF", "agentic AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
